"use client";
import { UserDataTypes } from "@/types";
import {} from "react";

function UpdateForm({ user }: { user: { data: UserDataTypes } }) {
  const { data } = user;

  return (
    <>
      <section></section>
    </>
  );
}

export default UpdateForm;
